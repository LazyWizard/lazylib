import org.junit.Test;

import java.text.NumberFormat;

public class ModUtilsTests
{
/*    private static final int NUM_TESTS = 150_000;

    @Test
    public void testGetCallerClass()
    {
        final NumberFormat format = NumberFormat.getNumberInstance();
        System.out.println("Running " + format.format(NUM_TESTS) + " tests...");
        long startTime = System.nanoTime();
        for (int i = 0; i < NUM_TESTS; i++)
        {
            final Class tmp = sun.reflect.Reflection.getCallerClass(2);
        }

        final double dur1 = (System.nanoTime() - startTime) / 1_000_000_000.0;
        System.out.println("Duration (getCallerClass()): " + format.format(dur1) + " seconds \t(" +
                (dur1 / NUM_TESTS) + " per)");

        startTime = System.nanoTime();
        for (int i = 0; i < NUM_TESTS; i++)
        {
            final String tmp = Thread.currentThread().getStackTrace()[2].getClassName();
        }

        final double dur2 = (System.nanoTime() - startTime) / 1_000_000_000.0;
        System.out.println("Duration (getStackTrace()):  " + format.format(dur2) + " seconds \t(" +
                (dur2 / NUM_TESTS) + " per)");
    }

    public static void main(String[] args)
    {
        new ModUtilsTests().testGetCallerClass();
    }*/
}
